export const environment = {
  production: false,
  baseURL: "http://localhost:4200",
  URL: "https://store.fe4its.com/api", // Change only the domain part, keeping "/api" intact
  storageURL: "", // Change only the laravel primary domain
};
