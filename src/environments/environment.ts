export const environment = {
  production: false,
  baseURL: 'http://localhost:4200',
  URL: 'https://laravel.pixelstrap.net/fastkart/api', // Change only the domain part, keeping "/api" intact
  storageURL: 'https://laravel.pixelstrap.net/fastkart' // Change only the laravel primary domain
};
